name: SonarCloud Analysis
on:
  workflow_run:
    workflows: ["PR Decoration, Dockerize Java App and Deploy on EC2 with ASG"]
    types:
      - completed
jobs:
  sonarqube:
    runs-on: 'self-hosted'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: 'zulu' # Alternative distribution options are available.
     
      - name: SonarQube analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}  # Your SonarQube authentication token
        run: |
          # Download and configure Sonar Scanner
          curl -L -o sonar-scanner-cli.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-x.y.z.zip
          unzip sonar-scanner-cli.zip
          export PATH=$PATH:$(pwd)/sonar-scanner-cli-x.y.z/bin

          # Run Sonar Scanner
          sonar-scanner \
            -Dsonar.projectKey=akshaykam_GitHub_Actions_Java_Angular_Mysql \
            -Dsonar.sources=. \
            -Dsonar.host.url={{ secrets.SONAR_HOST_URL }} \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}

     
